SubclassedView = require './fixtures/subclassed-view'

describe "SubclassedView", ->
  it "calls render with the alternative-renderer", ->
  it "overrides the title", ->
  it "overrides the icon", ->
